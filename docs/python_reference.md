# Python API Reference

The user-facing dataset and element reference has been merged into
[Python DataSet Access Guide](python_dataset_access_design.md).

Use that page for:

- the recommended Python access patterns
- `DataSet` / `DicomFile` / `DataElement` behavior
- value reads and writes
- presence checks
- explicit VR assignment for private tags
- `Tag`, `VR`, `Uid`, and `PersonName` usage

For a shorter entry page, see [Python API Overview](python_api.md).
